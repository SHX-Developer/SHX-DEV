# SHX DEV

## Локальный запуск

Проект лежит в папке `site`.

```powershell
cd "site"
npm install
npm run dev
```

После запуска открой адрес, который покажет Vite в терминале. Обычно это `http://localhost:5173`.

## Сборка

```powershell
cd "site"
npm run build
npm run preview
```
